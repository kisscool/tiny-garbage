
#resultsDiv
  %table{:id => "results"}
    %tr
      %th Name
      %th Number of files
      %th Size
      %th Last scan
      %th Online?
    - if not @ftp_list.nil?
      - @ftp_list.each do |ftp|
        %tr
          %td
            %a{:title => ftp.name, :href => ftp.url}= ftp.name
          %td
            = ftp.number_of_files
          %td{:class => "resultsSize"}
            = number_to_human_size(ftp.size)
          %td
            = ftp.updated_on
          %td
            = ftp.is_alive


